<Window x:Class="ZooApp.Views.AddExchangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Exchange Record"
        Height="450" Width="420"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="Select Animal:" Margin="0,5"/>
            <ComboBox x:Name="AnimalBox"
                      Margin="0,5"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Id"/>

            <TextBlock Text="Exchange Date:" Margin="0,5"/>
            <DatePicker x:Name="DateBox" Margin="0,5"/>

            <TextBlock Text="Exchange Type:" Margin="0,5"/>
            <ComboBox x:Name="TypeBox" Margin="0,5">
                <ComboBoxItem Content="sent"/>
                <ComboBoxItem Content="received"/>
            </ComboBox>

            <TextBlock Text="Other Zoo:" Margin="0,5"/>
            <TextBox x:Name="ZooBox" Margin="0,5"/>

            <TextBlock Text="Reason:" Margin="0,5"/>
            <TextBox x:Name="ReasonBox"
                     Margin="0,5"
                     Height="60"
                     TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="💾 Save" Width="110" Margin="5" Click="Save_Click"/>
            <Button Content="✖ Cancel" Width="110" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>