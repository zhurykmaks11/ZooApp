<Window x:Class="ZooApp.Views.EditFeedingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Feeding"
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Animal Name:"/>
            <TextBox x:Name="AnimalNameBox" Margin="0,5" />

            <TextBlock Text="Feed Type:"/>
            <TextBox x:Name="FeedTypeBox" Margin="0,5" />

            <TextBlock Text="Quantity (kg):"/>
            <TextBox x:Name="QuantityBox"
                     Margin="0,5"
                     PreviewTextInput="NumberOnly"
                     CommandManager.PreviewExecuted="QuantityBox_PreviewExecuted"/>

            <TextBlock Text="Feeding Time:"/>
            <TextBox x:Name="FeedingTimeBox" Margin="0,5" />

            <TextBlock Text="Season:"/>
            <TextBox x:Name="SeasonBox" Margin="0,5" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="100" Margin="5" Click="Save_Click"/>
            <Button Content="Cancel" Width="100" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>