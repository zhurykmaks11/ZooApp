<Window x:Class="ZooApp.Views.ViewCageAnimalsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Animals in Cage" Height="300" Width="400" WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <DataGrid x:Name="AnimalsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Species" Binding="{Binding Species}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>